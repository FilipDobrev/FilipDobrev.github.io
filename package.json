{
  "name": "melotech-site",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && wrangler dev",
    "deploy": "node build.js && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
